# docs-mcp-server Startup Rule

If the `docs-mcp-server` MCP server is not running or fails to connect, start it with the following command before proceeding:

```
npx @arabold/docs-mcp-server@latest
```

Run this command in a terminal and wait for the server to be ready before retrying any tool calls that depend on it.
